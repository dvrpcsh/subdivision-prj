# ===================================================================
# ?? ?? (prod profile) ??
# ===================================================================

# === DATABASE (AWS RDS - MySQL) ?? ===
# ?? ?? DB ??? ??, ??? ??, ????? ?? ????? ?????.
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === JPA(ORM) ?? ===
# ?? ????? ?? create? update ?? validate? ????,
# ??? DB ???? ????? ??? ??? ???? ?? ?????.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# === JWT ?? ===
# JWT ??? ?? ?? ????? ???? ?????.
jwt.secret=${JWT_SECRET}
jwt.expiration-ms=3600000

# === ?? ?? ?? ===
# ?? ????? ?? INFO ??? ???? ???? ??? ????.
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO
logging.level.com.subdivision.subdivision_prj=INFO

# === AWS S3 ?? ===
# ?? ??? ??? ?? ??? ???? ?????.
cloud.aws.s3.bucket=nottori-bucket-sanghyeop250818
cloud.aws.region.static=ap-northeast-2

# === AWS Credentials ===
# AWS ?? ?? ??? ?? ?? ????? ?????.
cloud.aws.credentials.access-key=${AWS_ACCESS_KEY}
cloud.aws.credentials.secret-key=${AWS_SECRET_KEY}

# === Email (SMTP) ?? ===
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# === Redis ?? ===
# ?? ??? Redis ?? ??? ?? ????? ?????.
spring.redis.host=${REDIS_HOST}
spring.redis.port=6379

# === OAuth2 ?? ===
# ?? OAuth2 ?? ??? ?? ????? ?????.
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}

spring.security.oauth2.client.registration.naver.client-id=${NAVER_CLIENT_ID}
spring.security.oauth2.client.registration.naver.client-secret=${NAVER_CLIENT_SECRET}
# ?? ????? ???? URI? ?? ??? ????? ???? ???.
spring.security.oauth2.client.registration.naver.redirect-uri=https://[????????]/login/oauth2/code/naver

spring.security.oauth2.client.registration.kakao.client-id=${KAKAO_CLIENT_ID}
spring.security.oauth2.client.registration.kakao.redirect-uri=https://[????????]/login/oauth2/code/kakao

# ... (??? OAuth2 ??? local? ??) ...
