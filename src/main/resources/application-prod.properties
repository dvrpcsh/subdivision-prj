# ===================================================================
# ?? ?? ?? ?? (prod profile)
# ===================================================================

# === DATABASE ?? ===
# ?? DB? URL, ??? ??, ????? ??? ?? ??? ?? ???? ?????.
spring.datasource.url=${DB_URL}&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# === JPA(ORM) ?? ===
# ? [?? ??] 'update'? ?? ???? ??? ?? ??? ?? ???.
# 'validate'? ????, ?????? ?? ? Java ???? DB ???? ?????? ????, ???? ???? ??? ???.
# ??? ??? ??? ?? Flyway? Liquibase ?? DB ?????? ??? ???? ?? ?? ?????.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false # ?? ????? SQL ??? ???? ?? ??? ?????.
spring.jpa.properties.hibernate.format_sql=false

# === JWT ??? ===
jwt.secret=${JWT_SECRET}

# === AWS S3 ?? ===
cloud.aws.s3.enabled=true
cloud.aws.s3.bucket=${AWS_S3_BUCKET}
cloud.aws.region.static=${AWS_REGION}
cloud.aws.credentials.access-key=${AWS_ACCESS_KEY}
cloud.aws.credentials.secret-key=${AWS_SECRET_KEY}

# === Email (SMTP) ?? ===
spring.mail.enabled=true # ?? ????? ??? ?? ??? ??????.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# === Redis ?? ===
spring.redis.host=${REDIS_HOST}
spring.redis.port=6379

# === OAuth2 ????? ?? ===
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile, email

spring.security.oauth2.client.registration.naver.client-id=${NAVER_CLIENT_ID}
spring.security.oauth2.client.registration.naver.client-secret=${NAVER_CLIENT_SECRET}
# ? Redirect URI? ?? ??? ???? ???? ????. (?: OAUTH2_REDIRECT_BASE_URI=https://api.mydomain.com)
spring.security.oauth2.client.registration.naver.redirect-uri=${OAUTH2_REDIRECT_BASE_URI}/login/oauth2/code/naver
spring.security.oauth2.client.registration.naver.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.naver.scope=name, email, profile_image
spring.security.oauth2.client.registration.naver.client-name=Naver

spring.security.oauth2.client.registration.kakao.client-id=${KAKAO_CLIENT_ID}
spring.security.oauth2.client.registration.kakao.redirect-uri=${OAUTH2_REDIRECT_BASE_URI}/login/oauth2/code/kakao
spring.security.oauth2.client.registration.kakao.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.kakao.scope=profile_nickname, account_email
spring.security.oauth2.client.registration.kakao.client-name=Kakao
spring.security.oauth2.client.registration.kakao.client-authentication-method=client_secret_post
```
